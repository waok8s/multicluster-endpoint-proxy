# permissions for end users to edit clusterscores.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: multicluster-endpoint-proxy
    app.kubernetes.io/managed-by: kustomize
  name: clusterscore-editor-role
rules:
- apiGroups:
  - multicluster-endpoint-proxy.waok8s.github.io
  resources:
  - clusterscores
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - multicluster-endpoint-proxy.waok8s.github.io
  resources:
  - clusterscores/status
  verbs:
  - get
