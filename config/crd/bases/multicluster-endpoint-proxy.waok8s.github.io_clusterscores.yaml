---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: clusterscores.multicluster-endpoint-proxy.waok8s.github.io
spec:
  group: multicluster-endpoint-proxy.waok8s.github.io
  names:
    kind: ClusterScore
    listKind: ClusterScoreList
    plural: clusterscores
    singular: clusterscore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .status.score
      name: Score
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ClusterScore is the Schema for the clusterscores API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterScoreSpec defines the desired state of ClusterScore
            properties:
              endpoint:
                type: string
              owncluster:
                default: false
                type: boolean
              region:
                type: string
            required:
            - endpoint
            type: object
          status:
            description: ClusterScoreStatus defines the observed state of ClusterScore
            properties:
              nodes:
                items:
                  properties:
                    name:
                      type: string
                    score:
                      format: int64
                      type: integer
                  required:
                  - name
                  - score
                  type: object
                type: array
              score:
                format: int64
                type: integer
              updateTimestamp:
                format: date-time
                type: string
            required:
            - nodes
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
